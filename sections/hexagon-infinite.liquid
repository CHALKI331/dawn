{% comment %}
  Infinite Hexagon Honeycomb
  - 4 fixed rows in a perfect honeycomb layout (no gaps)
  - Each hexagon is clickable and displays a product’s featured image
  - Each row is duplicated for seamless horizontal scrolling
{% endcomment %}

<div class="hexagon-scroll-container">
  <div class="hexagon-grid">
    <!-- Row 0 -->
    <div class="hexagon-row row-0">
      {% for product in collections.all.products %}
        <a href="{{ product.url }}" class="hexagon">
          {% if product.featured_image %}
            <img src="{{ product.featured_image | img_url: '400x400' }}" alt="{{ product.title }}">
          {% else %}
            <div class="placeholder">No Image</div>
          {% endif %}
        </a>
      {% endfor %}
      {% for product in collections.all.products %}
        <a href="{{ product.url }}" class="hexagon">
          {% if product.featured_image %}
            <img src="{{ product.featured_image | img_url: '400x400' }}" alt="{{ product.title }}">
          {% else %}
            <div class="placeholder">No Image</div>
          {% endif %}
        </a>
      {% endfor %}
    </div>

    <!-- Row 1 -->
    <div class="hexagon-row row-1">
      {% for product in collections.all.products %}
        <a href="{{ product.url }}" class="hexagon">
          {% if product.featured_image %}
            <img src="{{ product.featured_image | img_url: '400x400' }}" alt="{{ product.title }}">
          {% else %}
            <div class="placeholder">No Image</div>
          {% endif %}
        </a>
      {% endfor %}
      {% for product in collections.all.products %}
        <a href="{{ product.url }}" class="hexagon">
          {% if product.featured_image %}
            <img src="{{ product.featured_image | img_url: '400x400' }}" alt="{{ product.title }}">
          {% else %}
            <div class="placeholder">No Image</div>
          {% endif %}
        </a>
      {% endfor %}
    </div>

    <!-- Row 2 -->
    <div class="hexagon-row row-2">
      {% for product in collections.all.products %}
        <a href="{{ product.url }}" class="hexagon">
          {% if product.featured_image %}
            <img src="{{ product.featured_image | img_url: '400x400' }}" alt="{{ product.title }}">
          {% else %}
            <div class="placeholder">No Image</div>
          {% endif %}
        </a>
      {% endfor %}
      {% for product in collections.all.products %}
        <a href="{{ product.url }}" class="hexagon">
          {% if product.featured_image %}
            <img src="{{ product.featured_image | img_url: '400x400' }}" alt="{{ product.title }}">
          {% else %}
            <div class="placeholder">No Image</div>
          {% endif %}
        </a>
      {% endfor %}
    </div>

    <!-- Row 3 -->
    <div class="hexagon-row row-3">
      {% for product in collections.all.products %}
        <a href="{{ product.url }}" class="hexagon">
          {% if product.featured_image %}
            <img src="{{ product.featured_image | img_url: '400x400' }}" alt="{{ product.title }}">
          {% else %}
            <div class="placeholder">No Image</div>
          {% endif %}
        </a>
      {% endfor %}
      {% for product in collections.all.products %}
        <a href="{{ product.url }}" class="hexagon">
          {% if product.featured_image %}
            <img src="{{ product.featured_image | img_url: '400x400' }}" alt="{{ product.title }}">
          {% else %}
            <div class="placeholder">No Image</div>
          {% endif %}
        </a>
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "InfiniteHex",
  "settings": [],
  "presets": [
    {
      "name": "InfiniteHex",
      "category": "Custom"
    }
  ]
}
{% endschema %}
